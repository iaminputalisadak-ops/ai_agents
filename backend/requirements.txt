# AI Voice Agent - Backend Dependencies (pinned versions)
flask==2.3.0
flask-cors==4.0.0
openai>=1.0.0
SpeechRecognition==3.10.0
pyttsx3==2.90
requests==2.31.0
python-dotenv==1.0.0
wikipedia==1.4.0

# PyAudio - optional, fails on Python 3.14 (no wheels) - app works without it (uses browser Web Speech API)
# pyaudio==0.2.13
